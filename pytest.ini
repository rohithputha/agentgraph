[pytest]
markers =
    guarantees: Contract and SLA guarantee tests for AgentTest
    integration: Optional integration tests that can hit external systems
    live: Tests that require real API keys/network
    agenttest_runtime: Enable real runtime interception patches for this test
